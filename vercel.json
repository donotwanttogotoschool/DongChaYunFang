{
  "version": 2,
  "build": {
    "env": {
      "RUSTUP_HOME": "/root/.rustup",
      "CARGO_HOME": "/root/.cargo"
    },
    "scripts": {
      "postinstall": "curl https://sh.rustup.rs -sSf | sh -s -- -y && source $HOME/.cargo/env && rustc --version && pip install --no-deps -r requirements.txt"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
